import Foundation
import SwiftUI

struct Constants {
    static let baseURL = "https://gta6walkthroughapp.shayveetake2.workers.dev"
    
    struct Colors {
        static let primary = Color(red: 0.8, green: 0.1, blue: 0.6) // Vice City Pink/Purple
        static let secondary = Color(red: 0.0, green: 0.8, blue: 0.8) // Vice City Teal
        static let background = Color.black
        static let cardBackground = Color(white: 0.1)
    }
}
